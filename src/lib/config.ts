// Single source of truth for the canonical site URL. Used by sitemap,
// metadataBase, the badge generator, and the outreach export script.
// Keep absolute (no trailing slash) — callers add their own path.
export const SITE_URL = "https://www.localspecialtycoffee.com";
